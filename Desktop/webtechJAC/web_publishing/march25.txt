Notes:

Forms => Chapter 17

all form elements are inline!
if you put a div, put it inside the <form> tag

label
fieldset => legend
textfield
password field
checkboxes
radio buttons => only one
select lists (dropdown)
scrollable (select more than one)
textarea
submit button
reset button
button
file input


form tag
action="where does it go? filename"
method="post", "get" (appended to url)

*optional
enc MIME type (multipurpose internet mail exchange)
uploading files: enctype="application/x-www-form-urlencoded"
accept-charset="what kind of charset to accept on forms"

<label for="id of corresponding element">Username:</label>
<input type="text" maxlength="20" value="default" readonly="readonly" />

type=

text => maxlength, value (all), readonly="readonly", disabled="disabled" (all), name="username" (all) (for the server to access the data), id, class, tabindex, accesskey="hotkey"
password => maxlength, readonly
checkbox *checked="checked"
radio || same name, forces only one
submit || 
reset || 
button => value(text in button)

<input type="radio" name="gender" value="F" id="female" />
<label for="female">Female</label>
and then name=gender again, different value for "M"

label for="x" and then <input id="x" />

<input type="file" name="picture" id="pic"


TEXTAREA
rows, cols, (or do it in css) name, <textarea>default value</textarea>, need a corresponding label tag

SELECT
<label for="provinces">Where do you live?</label>
<select name="province" id="province">
<option>Quebec</option>
<option *value="ont (what the server gets)" if none, gets text>Ontario</option>

SCROLLABLE add the multiple="multiple" attribute
size is the number of visible options in a SELECT tag
</select>

OWN IMAGES for submit buttons
<input type="image" src="SOURCE" alt="alttext" *value="valueforserver"/>

